
# db
spring.datasource.url=jdbc:postgresql://localhost:5432/demo
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# Flyway Configuration
# ===============================
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.validate-on-migrate=false

server.port=8080

# ===============================
# HIKARI CONNECTION POOL
# ===============================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000

# ===============================
# Redis
# ===============================
spring.redis.host=redis
spring.redis.port=6379
spring.cache.type=redis

logging.level.org.springframework.cache=DEBUG
logging.level.org.springframework.cache.interceptor=DEBUG
logging.level.org.springframework.data.redis=DEBUG
logging.level.org.springframework.data.redis.core=DEBUG
logging.level.org.springframework.data.redis.cache=DEBUG

# ===============================
# JWT CONFIG
# ===============================
app.jwt.secret=Hj2n9kL0sQw8YfZt3M6vRp1aXyC4uE7B
app.jwt.expiration-ms=86400000

# Cáº¥u hÃ¬nh MyBatis
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.demo.model

# ===============================
# LOGGING SQL
# ===============================
# Hiá»n thá» cÃ¢u query trong log
logging.level.org.mybatis=DEBUG
logging.level.com.example.demo.repository=DEBUG
logging.level.jdbc.sqlonly=DEBUG
logging.level.jdbc.sqltiming=TRACE

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=luongvietvinh@gmail.com
# IMPORTANT: Use an "App Password" from Google, not your regular password
spring.mail.password=xgvm dwvh iefa hlkq 
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# AWS
# ===============================
aws.access-key=${AWS_ACCESS_KEY_ID:dumy-key}
aws.secret-key=${AWS_SECRET_ACCESS_KEY:dumy-key}
aws.region=${AWS_REGION:dumy-key}
aws.s3.bucket-name=${AWS_S3_BUCKET_NAME:dumy-key}
# Giới hạn upload file
spring.servlet.multipart.max-file-size=8388608
spring.servlet.multipart.max-request-size=8388608
